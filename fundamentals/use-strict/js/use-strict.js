/*
	The "use strict" directive switches the engine to the “modern” mode, changing the behavior of some built-in features. To work in the whole script, it has to be put before every command. It can also be put in specific classes and functions.
*/
"use strict";

alert('some code...');